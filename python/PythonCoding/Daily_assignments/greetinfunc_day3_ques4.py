# ques 4- Write a user-defined function factorial(n) that takes a positive integer n as an argument
# and returns its factorial. Use the function in a program and print the factorial of a given
# number.

def greet(name):
    print("Hello", name + "!")
name = input("Enter name: ")
greet(name)